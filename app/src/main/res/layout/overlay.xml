<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/holo_blue_light"
    android:padding="8dp">

    <TextView
        android:id="@+id/close"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:padding="12sp"
        android:textSize="15sp"
        android:text="close"/>

    <TextView
        android:id="@+id/word"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:paddingBottom="8sp"
        android:paddingTop="8sp"
        android:text="I am overlay"
        android:textColor="@android:color/white"
        android:textSize="14sp"/>

    <ImageView
        android:id="@+id/tts_button"
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:paddingBottom="4sp"
        android:paddingTop="4sp"
        android:layout_toRightOf="@id/word"
        android:src="@android:drawable/ic_lock_silent_mode_off"/>

    <TextView
        android:id="@+id/word_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/word"
        android:layout_toLeftOf="@id/close"
        android:text="Verb"
        android:textColor="@android:color/white"
        android:textSize="11sp"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/meaning"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/word_type"
        android:breakStrategy="balanced"
        android:paddingBottom="8sp"
        android:paddingTop="8sp"
        android:text="is an 2014  soap series that aired on Hum TV. It is directed by  while written by veteran writer and poet .
         Series was produced by   at  Productions."
        android:textColor="@android:color/white"
        android:textSize="12sp"/>

</RelativeLayout>
